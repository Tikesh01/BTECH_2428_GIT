#include<stdio.h>
struct Node
{
    int data;
    struct Node *next;
};
int main()
{
    struct Node *head;
    printf("Enter The Linked-List");
    for (int i = 0; i < 5; i++)
    {
        scanf("%d ", head->data);
        head->next = 
    }
    

    return 0;
}